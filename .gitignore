# Claude tooling (never commit into project repos)
.claude/
CLAUDE.md
.superpowers/
.worktrees/

# local docs (not published)
docs/

# runtime / large artifacts
__pycache__/
*.pyc
*.log
hst/hts_cache/
images/

# SQLite corpora + their WAL/shm sidecars (large, regenerable)
hst/*.db
hst/*.db-wal
hst/*.db-shm
hst/*.db.bak

# NVFP4 / 27B experiment scaffolding (external, non-runnable on this box)
nvfp4/

# source binaries (large, regenerable / external)
*.pdf
file
